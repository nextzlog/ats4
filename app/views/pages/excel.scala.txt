@()(implicit db: Database)

@MVP(minor: Minor) = @{if(minor.place == 0) "優勝" else "%d位".format(1+minor.place)}

呼出符号,運用場所,参加部門,順位,入賞,得点,名前,住所,アドレス,コメント
@for(sect <- Sections.all; (major,minor) <- Minor.ofCall(sect.getName).sortBy(-_.total).map(m=>Major.ofCall(m.call).get->m)) {
@major.call,@minor.city,@minor.sect,@MVP(minor),@if(minor.award){入賞局},@minor.total,@major.name,@major.addr,@major.mail,@major.comm
}
